# Synthetic Data Schemas

The canonical Phase 3 CSV schemas are defined in:

```text
packages/synthetic-data/kra_synthetic/contracts.py
```

The generator writes one CSV per source table plus `risk_scenarios.csv`,
`risk_scenarios.json`, and `manifest.json`.
